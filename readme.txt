In our code we used minimise from scipy.optimize.minimize to get the values of alpha.
We need to put the data file 'diabetes.csv' in the same directory of the python file.
The number of iterations of the minimizer is set to 10 to lower the runtime of the code.
